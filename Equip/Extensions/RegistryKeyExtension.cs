using Microsoft.Win32;

namespace Microsoft.Win32
{
    public static  class RegistryKeyExtension
    {
        //public static string GetKeyValue(this RegistryKey registryKey, string key)
        //{
        //    if (registryKey == null)
        //        return null;
        //    return registryKey.GetValue(key).ToString();
        //}

        //public static RegistryKey GetRegistryKey(this RegistryKey registryKey, string key)
        //{
        //    return registryKey.OpenSubKey(key);
        //}
    }
}
